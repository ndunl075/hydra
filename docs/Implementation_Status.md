# Implementation status

Features ship in separate branches and PRs. Type checking, builds, appropriate tests, and packaging must pass before a feature is merged.

| Feature | State | Evidence / limits |
| --- | --- | --- |
| UI palette and appearance | Implemented; automated Windows native checks passed | Black and white dark primary, dark green secondary; Hydra Dark and Hydra Light themes; in-IDE Settings changes appearance only on explicit choice; manager follows native light/high-contrast modes. Host tests verify native theme switching, settings-tab reuse, workspace-override refusal, and preserved unsaved text, terminal process, and task records; manual visual/accessibility acceptance remains pending |
| M0 mode prototype | Implemented; automated Windows checks passed | Status bar, shortcut, React manager, safe native text-tab behavior. Three mode cycles preserve unsaved text, selection, focus, and the existing terminal process |
| Agent map | Implemented; browser checks passed; native CI required before merge | Compact repository → worktree → assigned-agent view; all task nodes selectable; observed managed activity animates, approvals pause the affected route, external sessions stay static. Collapse, pause, reduced motion, 30-task scrolling, light/dark and narrow layout checked. No inferred agent-to-agent dependencies, message traffic, or model calls |
| M1 worktrees and terminals | Foundation implemented; Windows automated checks passed | Three isolated tasks, both terminal launch routes, concurrency limit, recovery, and ownership tested. Real-provider sessions still require acceptance validation; terminal and managed launches now share a persistent queue with the existing default limit of two; uncertain writers reserve capacity after restart until explicit reconciliation. Scheduler fixtures cover dependencies, reviewed results, selected commits, cancellation, and duplicate-launch refusal; combined native acceptance remains a CI gate |
| M2 official extensions | Handoff foundation implemented; authenticated acceptance pending | Generated single-worktree workspaces for both providers; public-command availability checks; explicit prompt copy; persistent external ownership and safe failure handling. Automated native workspace loading tested. No automatic transcript transfer or observed external completion; real-provider/history validation pending |
| M3 structured sessions | Claude and pinned Codex structured foundations implemented; milestone incomplete | CLI 2.1.270 streamed text, explicit session-ID follow-up, and usage confirmed with a real tools-disabled scratch test. Windows fixture/host tests cover persistence, recovery, malformed output, writer exclusion, and process-tree stop. Codex 0.154.0 adapter adds stable initialization, streaming, explicit thread resume, scoped command/file/network approvals, readiness checks, and protocol interrupt with owned-tree fallback. Real Codex schema/initialization checked; authenticated model/tool acceptance, broader approval prompts, Claude interactive approvals/graceful interruption, and full interrupted-session acceptance remain pending |
| M4 review and integration | Guarded integration merged in PR #22 after Linux and native checks; discard pending | Prepared reviews bind exact task trees to commit receipts. Detached integration candidates retain task/target checkouts on conflict or failed/cancelled acceptance commands, require committed resolution review, and fast-forward through the clean owning target checkout after stale-state and dirty-buffer checks. Journals, shared repository locks and rollback references preserve recovery. Real-Git, browser and native command/diff/promotion checks passed for that feature revision. Later combined revisions retain their own acceptance gates. Confirmed discard remains pending; see [Integration.md](Integration.md) |
| Task context and usage | Implemented on the feature stack; native gate pending | Explicit briefs lock durably before queue submission; users can inspect the exact saved initial prompt and distinguish unsaved drafts. Local handoff notes link full evidence without a summarization call and preserve unsaved generated-file edits. Task/project usage uses complete durable histories with provider-specific cumulative accounting, independent of display truncation. Suggested test commands remain advisory; no budget enforcement or measured savings claim. See [Task_Context_And_Usage.md](Task_Context_And_Usage.md) |
| Model and effort controls | Managed Codex controls implemented on the feature stack; native gate pending | Runtime catalog discovery, saved model/effort, effective thread acknowledgement before turn submission, resume checks and rerouting evidence are implemented. Defaults remain provider-owned. Astra High requires the exact advertised model and effort; catalog availability is not account entitlement. Verified Claude effort, live model acceptance and quotas remain pending. See [Model_Controls.md](Model_Controls.md) |
| M5 reliability and efficiency | Partially implemented | Durable queue/session recovery, writer ownership, integration journals and reported usage provide foundations. Cross-window global capacity, resource setup, launch/turn budgets and measured efficiency remain incomplete. Worktrees do not isolate ports, databases, services or all filesystem access |
| M6 desktop delivery and onboarding | Standalone build and onboarding merged; account setup and installer gates pending; milestone incomplete | Branded standalone acceptance passed in PR #11. Active-profile import preserves conflicts, backups and undo. Replayable, skippable onboarding passed Linux/native checks and merged in PR #18. Provider-owned account setup and the Windows user installer are implemented on the stack; their exact-revision gates remain pending. Live sign-in, signing/distribution, distinct-version upgrades, automatic updates and manual wizard/accessibility acceptance remain outstanding. See [Desktop_Delivery.md](Desktop_Delivery.md); a core .vsix is not the IDE release |

## Acceptance record

Snapshot for combined feature-stack revision `1410251`, recorded on 17 September 2026. This revision contains pending features; presence in the stack does not mean they have merged or passed their final native gate.

| Feature PR | Recorded gate state |
| --- | --- |
| [#18: Replayable onboarding](https://github.com/ndunl075/hydra/pull/18) | Linux and native checks passed; merged |
| [#22: Reviewed integration](https://github.com/ndunl075/hydra/pull/22) | Linux and native checks passed; merged |
| [#20: Persistent scheduling](https://github.com/ndunl075/hydra/pull/20) | Exact-revision native acceptance pending |
| [#21: Briefs, handoffs and usage](https://github.com/ndunl075/hydra/pull/21) | Exact-revision native acceptance pending |
| [#23: Codex model and effort](https://github.com/ndunl075/hydra/pull/23) | Exact-revision native acceptance pending |
| [#24: Provider accounts](https://github.com/ndunl075/hydra/pull/24) | Exact-revision native acceptance pending |
| [#19: Windows installer](https://github.com/ndunl075/hydra/pull/19) | Final combined native/build/installer lifecycle gates pending; an older-revision run does not certify this stack |

Combined revision `1410251` passed local type checking, the build and all 84 tests on Windows (123 seconds). Installer revision `16d0462` passed native app build/smoke and installer generation; its lifecycle run remains pending and does not certify the final combined revision. Feature documents retain their earlier bounded fixture and browser evidence. Fixtures do not establish live sign-in, authenticated model/tool acceptance, subscription eligibility, or measured token savings. Update this record with exact revisions and completed native runs before final release acceptance.

## Layout decision

Use supported editor webviews alongside native editor groups and terminals. M0 does not rearrange the editor grid, close text tabs, or manipulate private DOM. Returning to Editor restores the previously active text editor and its selection and visible range. Native terminals are not recreated by the toggle.

Exact restoration of every arbitrary editor/sidebar/panel layout is not claimed. Full multi-pane layout and manual accessibility checks remain part of the feasibility gate.
