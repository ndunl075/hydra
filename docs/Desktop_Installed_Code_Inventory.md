# Installed-code inventory preparation

The Windows x64 user-installer task now writes `HydraInstalledInventory.json` beside `HydraSetup.exe`. It is a deterministic, **unsigned build-evidence artifact** describing every file selected by the pinned Inno `[Files]` mapping for Hydra's normal unversioned user install. This is preparation for a future owner-signed installed-code trust record; the current helper and update channel remain disabled.

The hook runs after the setup task clears its output directory and writes installer-specific `product.json` with `target: "user"`, and before Inno compiles the installer. It includes the final executable and visual manifest, application scripts, native libraries, built-in extensions, resources, direct installer tools and policies, and that staged product record. It excludes the source tree's `product.json`, signing-summary notes, and any file that the pinned mapping excludes. Versioned, proxy, Appx, and other installer layouts refuse until mapped and reviewed. `scripts/desktop.mjs installer` compares the saved inventory with the inputs again after Inno exits and refuses differences observed by the two scans. It does not hold file and directory leases across Inno compilation; a transient replacement between scans is not ruled out.

Schema version 1 records Hydra version, user x64 target, source and pinned upstream commits, and an ordinal-sorted list of destination path, byte length, and SHA-256. The parser requires canonical JSON, exact fields, bounded counts and sizes, unique case-insensitive Windows-safe paths, and valid digests. Generation refuses symbolic links/reparse entries, multiple hard links, unsupported filesystem objects, missing required installer tools, case collisions, and changes observed through a file handle while it is hashed. `npm.cmd run test:installed-inventory` exercises synthetic installer inputs and tampering. `verify-tree` compares an exact payload-only tree with the manifest; it does not yet account for Inno-generated uninstall files, shortcuts, or user-created data in a real installation.

This document and its commit fields are **not authentication**. A same-user process can modify an unsigned manifest and installed JavaScript. The release design must first sign final payload executables, hash the final signed bytes, sign the full inventory under a compiled/reviewed trust root, embed that signed inventory in the installer, then compile/sign the installer and publish signed metadata binding its final hash and the inventory. The inventory and its signature must be excluded from their own file list. The current 64 KiB update-feed payload and its three-file `expectedFiles` field are not a substitute for this separate complete artifact. Exact packaged-to-installed equivalence and controlled signed release acceptance still require a disposable Windows installer run.

## Native detached-signature preparation

`native/desktop-installed-inventory-signature.cpp` now has a bounded Windows CNG byte verifier for a separate installed-inventory signature. The fixture-only policy accepts a 104-byte record containing `HYDRINV1`, the SHA-256 key identifier, and a 64-byte ECDSA P-256/SHA-256 signature in IEEE P1363 `r || s` form. It signs the domain `Hydra installed-code inventory signature v1` followed by a NUL byte, the 40-byte record header, a 32-bit little-endian inventory byte length, and the exact inventory bytes. The test generates an ephemeral P-256 key and compiles only its public key into a separate fixture executable. `scripts/desktop-native-inventory-signature-test.ps1` exercises real CNG verification and refusal of altered bytes, key identifiers, signatures, formats, and size bounds.

This verifier authenticates and returns retained bytes only. A valid signature over malformed JSON still passes its cryptographic check; the strict inventory parser must reject those bytes separately. The production entry point always refuses because no owner-approved inventory key is compiled. It is not linked into the packaged helper, and the unsigned build inventory remains unsigned. Later work must bind owner-signed inventory bytes to the exact installed tree and to the code actually loaded by Electron main before a helper request can be trusted. No installed-code or signed-release acceptance is claimed by the fixture test.

## Native schema preparation

`native/desktop-installed-inventory-schema.cpp` validates the retained, signature-verified bytes against the compact field order emitted by the installer inventory generator. It checks the exact v1 product, target, and layout; version and commit formats; file count and size limits; strict UTF-8 paths; Windows-safe segments; ordinal ordering; and case-insensitive uniqueness. It returns parsed file lengths and digests for a later native installed-tree comparison. This native release grammar is intentionally narrower than the build script's general JSON parser: re-ordered objects and JSON escapes are refused even when they encode the same data. The generator emits neither form. `scripts/desktop-native-inventory-schema-test.ps1` signs both valid and malformed fixture bytes with an ephemeral key, verifies their signatures through Windows CNG, and then checks schema acceptance or refusal.

The parser neither reads nor leases installed files. A disk inventory, even with a valid owner signature, cannot prove which JavaScript Electron main already loaded; an altered script could run and then restore the disk bytes. The helper remains disabled until a reviewed startup-to-handoff provenance mechanism and exact installed-tree verification exist.

## Native payload-tree snapshot

`native/desktop-installed-inventory-tree.cpp` now compares a fixture-signed and parsed inventory against an exact payload-only Windows tree. It opens a canonical drive-letter root through retained, non-reparse ancestor handles; enumerates only expected directories and files; rejects extra, missing, linked, reparsed, or changed payloads; hashes each file through the handle it keeps; and returns a move-only lease on success. `scripts/desktop-native-inventory-tree-test.ps1` exercises a root file, nested file, tampering, missing and extra entries, a hard link, a junction, and write/rename attempts on leased files. This test runs in the Windows desktop workflow.

The file leases prevented rewriting and renaming already verified files in the local fixture, but **did not prevent creating a new file in the leased directory**. A fresh tree check rejects the addition; the original lease alone does not keep the namespace exact. The fixture's root argument is not a production authority, and a real Inno installation also contains installer-generated files that the exact payload-only test does not yet model. This is a verified snapshot primitive, not an installed release acceptance or a proof of code loaded by Electron. The production helper remains unlinked and refusal-only.
