# Hydra landing page

A single self-contained page (`index.html` plus the logo). No build step and no dependencies.

Preview locally:

```
npx serve site
```

To publish with GitHub Pages, point Pages at a branch and select this folder as the source, or copy the two files wherever static hosting lives.
